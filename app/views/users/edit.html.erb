<h1>プロフィール編集</h1>

<%= form_with model: @user do |f| %>
  <%= f.text_field :name %>
  <%= f.email_field :email %>
  <%= f.collection_select(:industry_id, Industry.all, :id, :name, {}, {class:""}) %>
  <%= f.submit "保存" %>
<% end %>