<div class="container">
  <ul class="list-group">
    <li class="list-group-item active">フォロー</li>
    <% @users.each do |user| %>
      <li class="list-group-item">
        <%= link_to "#{user.name}",user_path(user.id) %>
      </li>
    <% end %>
  </ul>
</div>